import React from 'react';

const About = () => {
    return (
        <div style={{width: '100%'}}>
            <h1>it is a simple react web app</h1>
        </div>
    );
};

export default About;